//  自行編輯  //
//  判斷是否為字元  //

#include <stdio.h>
#include <ctype.h>

int main () {
   
   char ch;
   scanf("%c", &ch);
    
   if(isgraph(ch)) {  //  如果是字元 isgraph()為1 若為空白字元則為0  //
      printf("%c is a graph\n", ch);
   } 
   else {
      printf("%c is not a graph\n", ch);
   }
   return 0;
}
