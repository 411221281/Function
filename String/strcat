//  自行編輯  //
//  連接字串  //

#include <stdio.h>
#include <string.h>

int main () {

   char str1[10], str2[10];
   strcpy(str1, "Hello ");
   strcpy(str2, "World!");

   strcat(str1, str2);  //  strcat()將字串2接在字串1的尾部  //

   printf("Whole String : %s", str1);
   return 0;
}
