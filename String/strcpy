#include <stdio.h>
#include <string.h>
 
int main () {

   char str1[15];
   char str2[15];
   int result;
 
   strcpy(str1, "abcdef");
   strcpy(str2, "ABCDEF");

   printf("%s\n%s\n", str1, str2);
   return 0;
}
